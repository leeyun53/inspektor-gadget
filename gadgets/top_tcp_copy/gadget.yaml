name: top tcp copy
description: Periodically report tcp send receive activity by connection
homepageURL: https://inspektor-gadget.io/
documentationURL: https://www.inspektor-gadget.io/docs/latest/gadgets/top_tcp_copy
sourceURL: https://github.com/inspektor-gadget/inspektor-gadget/tree/main/gadgets/top_tcp_copy
datasources:
  tcp_copy:
    annotations:
      cli.clear-screen-before: "true"
    fields:
      received:
        annotations:
          description: Received bytes
      received_count:
        annotations:
          description: Received frequency count
      sent:
        annotations:
          description: Sent bytes
      src:
        annotations:
          template: l4endpoint
      dst:
        annotations:
          template: l4endpoint
params:
  ebpf:
    target_family:
      key: family
      defaultValue: "-1"
      description: Trace traffic from the specified IPv family. Can be either 4 or 6. Specifying -1 equals to no filtering
    target_pid:
      key: pid
      defaultValue: "0"
      description: Show only events generated by process with this PID
